'use strict';

module.exports = {
	server: {
		host: 'localhost',
		port: 7002
	},
	//userId: '', // User id from DrEdition, normally provided in the url
	dredition: {
		url: 'https://dredition-api.aptoma.no',
		apikey: '' // Needs read access to the product, can also be provided in the preview url
	}
};
